{
  "scripts": {
    "deploy": "ts-node src/deploy-commands.ts",
    "start": "ts-node src/index.ts",
    "update": "git add yarn.lock && git commit -m \"dependencies update\" && git pull origin && git push origin"
  },
  "dependencies": {
    "discord.js": "^14.8.0",
    "quick.db": "^9.0.8"
  },
  "devDependencies": {
    "@types/node": "^18.14.6",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "resolutions": {
    "discord-api-types": "0.37.20"
  }
}
